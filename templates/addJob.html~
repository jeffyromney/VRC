{% extends "base.html" %}

{% block title%}Add Jobs{% endblock %}

{% block content %}
{% if form.errors %}
    {% for field in form %}
        {% for error in field.errors %}
            <div class="alert alert-error">
                <strong>{{ field.name }} - {{ error|escape }}</strong>
            </div>
        {% endfor %}
    {% endfor %}
    {% for error in form.non_field_errors %}
        <div class="alert alert-error">
            <strong>{{ error|escape }}</strong>
        </div>
    {% endfor %}
{% endif %}
    <form action="" method="post">
    Start date {{ form.sdate }}<br>
    End date {{ form.edate }}<br>
    Event name {{ form.event }}<br>
    Agency Name {{ form.agency }}<br>
    Title of Volunteer position {{ form.title }} (How is this different from Event name?)<br>
    Duties {{ form.duties }}<br>
    Volunteer must be able to lift {{ form.weight }} lbs.<br>
    Volunteer must be able to drive {{ form.drive }}<br>
    Number of volunteers needed {{ form.number }}<br>
    Dates/hours needed {{ form.whenneeded }}<br>
    Minimum age {{ form.age }}<br>
    Needed skills{{ form.skills }}<br>
    Other Notes {{ form.other }}<br>
        {% csrf_token %}
        {% if not viewOnly %}<input type="submit" value="Submit"> {% endif %}
    </form>
{% endblock %}
